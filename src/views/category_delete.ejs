<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/output.css" />
  <title>Grocery | <%= title %>
  </title>
</head>

<body class="dark:bg-black dark:text-white grid">
  <div class="grid md:grid-cols-[auto_1fr]">
    <%- include("./sidebar") %>
      <main class="space-y-4 p-8">
        <h1 class="text-5xl font-bold">
          <%= title %>: <%= category.name %>
        </h1>
        <% if (category_items.length) { %>
          <p class="text-xl">
            <strong>
              Delete the following items before attempting to delete this category.
            </strong>
          </p>
          <div>
            <h4 class="text-3xl font-semibold">Items</h4>
            <dl class="space-y-2 p-4 bg-zinc-500">
              <% category_items.forEach((item)=> { %>
                <dt class="text-xl font-medium">
                  <a href=<%=item.url %>>
                    <%= item.name %>
                  </a>
                </dt>
                <dd>
                  <%= item.description %>
                </dd>
                <% }); %>
            </dl>
          </div>
          <% } else { %>
            <p>Do you really want to delete this category?</p>
            <form method="post">
              <div>
                <input type="hidden" id="categoryid" class="" name="categoryid" value=<%=category._id %>>
              </div>
              <button class="m-auto p-4 text-xl fond-medium bg-white text-black rounded-xl" type="submit">
                Delete
              </button>
            </form>
            <% } %>
      </main>
  </div>
</body>

</html>