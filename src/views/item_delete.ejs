<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/output.css" />
    <title>Grocery | <%= title %>
    </title>
</head>

<body class="dark:bg-black dark:text-white grid">
    <div class="grid md:grid-cols-[auto_1fr]">
        <%- include("./sidebar") %>
            <main class="space-y-4 p-8">
                <h1 class="text-5xl font-bold">
                    <%= title %>: <%= item.name %>
                </h1>
                <p>Do you really want to delete this item?</p>
                <form method="post">
                    <div>
                        <input type="hidden" id="itemid" class="" name="item_id" value=<%=item._id %>>
                    </div>
                    <button class="m-auto p-4 text-xl fond-medium bg-white text-black rounded-xl" type="submit">
                        Delete
                    </button>
                </form>
            </main>
    </div>
</body>

</html>